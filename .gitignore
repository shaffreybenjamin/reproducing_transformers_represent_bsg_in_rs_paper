# Nested Astera library — cloned fresh on the GPU pod, not part of this repo
simplexity/

# Trained checkpoints (~700KB) and figures (~6MB) ARE committed so others can
# reproduce/inspect results without retraining — see runpod/train.sh. Tiny enough
# for plain git; no LFS needed.

# Python caches & logs
__pycache__/
*.pyc
*.log

# Local RunPod secrets (created later)
runpod/config.env

# Local CPU venv for analysis/plotting (created by uv; do not commit)
.venv/
